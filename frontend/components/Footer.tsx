export function Footer() {
  return (
    <footer className="border-t border-slate-200 bg-white">
      <div className="mx-auto flex max-w-7xl flex-col gap-2 px-4 py-6 text-sm text-slate-500 sm:flex-row sm:items-center sm:justify-between sm:px-6">
        <p>VigiReal is a decision-support prototype, not an official public-health advisory.</p>
        <p>Use agency notices and local health department guidance as the source of truth.</p>
      </div>
    </footer>
  );
}
